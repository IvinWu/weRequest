export default {
    session: '' as string,
    // session过期的时间点
    sessionExpire: Infinity as number,
    // 是否启用备用域名
    isEnableBackupDomain: false,
    // 当前是否在登录流程中
    isLoginning: false,
} as any
